{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "AetherOS 桌面端默认能力：主窗口对前端暴露的权限集",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
