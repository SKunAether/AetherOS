# AetherOS 2.0 环境变量示例文件
# 复制为 .env 并按需配置；所有变量均可省略，存在默认值。

# 更新清单源 URL（覆盖程序内捆绑的更新源；支持 http/https 与 file:// 本地源）
# AETHEROS_UPDATE_MANIFEST_URL=https://github.com/SKunAether/AetherOS/releases/latest/download/update-manifest.json

# 日志级别：trace | debug | info | warn | error
# AETHEROS_LOG=info

# 数据根目录（默认 %LOCALAPPDATA%\AetherOS），主要用于测试隔离
# AETHEROS_DATA_DIR=
